server {
  listen 80;
  listen [::]:80;

  server_name .{{ domain }};

  # ACME-challenge
  location ^~ /.well-known/acme-challenge/ {
    root /var/www/_letsencrypt;
  }

  location / {
    return 301 https://{{ domain }}$request_uri;
  }
}
